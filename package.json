{
  "name": "melchua.com",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "rm -rf _site/ && npm install && npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "write": "NODE_NO_WARNINGS=1 node ./scripts/create-post.js",
    "deploy": "git add -A . && git commit -m '[auto-deploy]' && git push origin master"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.2"
  },
  "dependencies": {
    "eleventy-plugin-sass": "^1.0.0",
    "html-minifier": "^4.0.0",
    "markdown-it": "^10.0.0",
    "markdown-it-block-embed": "0.0.3",
    "open-editor": "^2.0.1",
    "slugify": "^1.4.0"
  }
}
