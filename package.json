{
  "name": "melchua.com",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "rm -rf _site/ && npm install && npx @11ty/eleventy --serve",
    "build": "npm install && npx @11ty/eleventy",
    "write": "NODE_NO_WARNINGS=1 node ./scripts/create-post.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.10.0"
  },
  "dependencies": {
    "eleventy-plugin-sass": "^1.0.0",
    "slugify": "^1.4.0"
  }
}
