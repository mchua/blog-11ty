---
layout: default
---

<h1>Writing</h1>

{%- for year in collections.postsByYear.years | reverse -%}
{%- set posts = collections.postsByYear.postMap[year] -%}
<details {% if loop.first %}open{% endif %}>
  <summary><h2>{{ year }} ({{ posts.length }}<span class="sr-only"> posts</span>)</h2></summary>

  <ul>
    {%- for post in posts | reverse -%}
    <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
    {%- endfor -%}
  </ul>
</details>
{%- endfor -%}